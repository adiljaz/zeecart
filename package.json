{
  "name": "zeecart",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd client && npm install",
    "build": "cd client && npm run build && mv dist ../dist",
    "dev": "cd client && npm run dev"
  }
}
